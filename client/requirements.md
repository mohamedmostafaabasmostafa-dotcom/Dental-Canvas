## Packages
framer-motion | 3D animations and page transitions
react-icons | Additional icons if Lucide isn't enough (optional, sticking to Lucide mostly)
zod | Schema validation
@hookform/resolvers | Form validation with Zod

## Notes
Using Framer Motion for 3D entrance animations.
Tailwind config will need extending for custom colors and fonts (DM Sans + Outfit).
Images will be sourced from Unsplash.
API hooks will be generated based on the provided schema/routes.
